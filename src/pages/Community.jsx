import {SafeAreaView} from 'react-native';
import BasicHeader from '../components/BasicHeader';

const Community = () => {
  return (
    <SafeAreaView>
      <BasicHeader title={'커뮤니티페이지'} />
    </SafeAreaView>
  );
};

export default Community;
